# ==============================================================================
# PARAMETERS — Paramètres de path réutilisables dans tous les fichiers
# ==============================================================================

parameters:
  collectiviteId:
    name: collectiviteId
    in: path
    required: true
    schema:
      type: string
      format: uuid
    description: Identifiant unique de la collectivité

  membreId:
    name: membreId
    in: path
    required: true
    schema:
      type: string
      format: uuid
    description: Identifiant unique du membre

  cotisationId:
    name: cotisationId
    in: path
    required: true
    schema:
      type: string
      format: uuid
    description: Identifiant unique de la cotisation

  compteId:
    name: compteId
    in: path
    required: true
    schema:
      type: string
      format: uuid
    description: Identifiant unique du compte

  activiteId:
    name: activiteId
    in: path
    required: true
    schema:
      type: string
      format: uuid
    description: Identifiant unique de l'activité
